# Roadmap

## 当前个人项目资产

- `停在十七岁/py/audit_style_sense.py`：语感事后审计。
- `停在十七岁/py/scan_repetition.py`：重复表达扫描。
- `停在十七岁/py/scan_cross_chapter_reuse.py`：跨章复用扫描。
- `停在十七岁/py/validate_chapter.py`：章节校验。
- `停在十七岁/py/parse_chapters.py`：章节解析。
- `停在十七岁/books/停在十七岁的人/写作规则库/`：《停在十七岁的人》的个人长篇规则库样例。

## 近期任务

1. 把脚本中的项目专名、目录路径和角色名抽成配置。
2. 将 `tools/audit_sample.py` 扩展为统一 CLI：`novel-humanize audit`。
3. 新增报告格式：Markdown + JSON 双输出。
4. 为重复扫描和语感审计补最小测试。
5. 写一份“如何把自己的个人网文接入本工具包”的教程。
6. 把脱敏示例扩展为单章审稿、卷级审稿和跨章复用三类。

## 质量原则

- 所有硬禁规则必须可追溯到项目规则库。
- 审计结果必须区分“必须处理”和“建议复核”。
- 脚本不能直接改正文，除非用户显式运行修复命令。
- 项目示例必须脱敏，避免公开个人小说非公开正文。
- 所有自动建议都要保留人工判断入口。
